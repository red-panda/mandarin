extends layout/app

block title
  title Мандарин

block main
  .user
    h1.user__name
      | Deynis
    p.user__mail
      | denis@jarency.ru

  section.cards
    ul.cards__list
      - var cards = [{title: 'Баланс', subTitle: 'На балансе: 75 000 р.'}, {title: 'Мои услуги', subTitle: 'Подключена', link: {title: '1 услуга', url: '#'}}, {title: 'Поддержка', subTitle: 'У вас', link: {title: '1 заявка', url: '#'}}]
      each val, index in cards
        li.cards__item
          .card
            .card__title
              = val.title
            .card__sub-title
              = val.subTitle + ' '
              if val.link
                a(href=val.link.url)
                  = val.link.title
            a.card-link(href="#")
              span.card-link__icon
              span.card-link__text
                | Пополнить счёт